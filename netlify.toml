[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Disable secret scanning for this specific key
[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_UNSPLASH_ACCESS_KEY"
