[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Disable secret scanning for these specific keys
[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_UNSPLASH_ACCESS_KEY,UNSPLASH_ACCESS_KEY"
